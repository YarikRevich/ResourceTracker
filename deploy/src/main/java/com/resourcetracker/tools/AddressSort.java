package com.resourcetracker.tools;

import java.util.Comparator;
import java.util.ArrayList;

// import com.resourcetracker.entities.Result;

/**
 * Implements comparator used for sorting array of addresses
 *
 * @author YarikRevich
 */
public class AddressSort {
    // final static Logger logger = LogManager.getLogger(ListenerPoll.class);

    // class AddressSortComparator implements Comparator<ListenerResult> {
    //     @Override
    //     public int compare(ListenerResult o1, ListenerResult o2) {
    //         return o1.index() > o2.index() ? 0 : 1;
    //     }
    // };

    // public static void sort(ArrayList<Result> src) {
    //     Collections.sort(src, new AddressSortComparator());

    //     for (Result v : src) {
    //         ListenerPoll.logger.info(v.message());
    //     }
    // }
}
