requests:
    - name: "first"
      frequency: "10 * * * *"
      file: "script.sh"
cloud:
    provider: "aws"
    credentials:
      file: "~/.aws/credentials"
      profile: "default"
      region: "default"
api-server:
    host: "http://localhost:8080"